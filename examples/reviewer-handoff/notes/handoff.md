# Agent Handoff

## Change

The example change updates a local documentation file and records the commands a reviewer should inspect.

## Evidence

- `artifacts/test.log` contains a sample passing check log.
- `git status --short` is collected by ProofDock during `collect`.
- `git ls-files` is collected to show the repository files visible to the bundle.

## Review Focus

Confirm that the real project replaces this sample note and log with task-specific evidence before sharing the proof bundle.

